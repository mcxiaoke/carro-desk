# 2026-09-16

- 23:22 托盘动态注入引擎 + 状态卡删除 + M3 祛强引用（§4.1/§4.4/§2.1/§3.2） — `src/Host/Services/DynamicTrayController.cs`(新) `src/Views/TrayContextMenu.xaml` `src/Views/TrayContextMenu.xaml.cs` `src/App.xaml.cs` `src/Views/ConfigEditorWindow.xaml.cs` `src/Modules/ScreenLock/ScreenLockModule.cs` `src/Modules/AudioSwitch/AudioSwitchModule.cs` `src/Modules/TaskScheduler/TaskSchedulerModule.cs` `src/Modules/AppAutoMute/AppAutoMuteModule.cs`：
  - **状态卡删除（§4.4）**：托盘顶部状态微卡及四态聚合（`SetStatus/RefreshStatus`）整体删除；`App.UpdateTrayText` 收敛为最简应用名 ToolTip，不再做四态聚合；"已暂停/已禁用"语义改由 ScreenLock 模块菜单项 `IsChecked`/`Header` 表达。
  - **双锚点动态注入（§4.1/§4.3）**：`TrayContextMenu.xaml` 保留 `PluginSlotAnchorTop/Bottom` 两锚点，新增 `Host/Services/DynamicTrayController` 把各模块 `GetTrayMenuItems()` 按 `Order` 插入两锚点之间；维护 `_inserted` 登记表精确移除（杜绝 `IndexOf(anchor)+1`）；空区间折叠相邻分隔线、折叠连续分隔；`RequestRefresh` 走 Dispatcher + 150ms 防抖、菜单 `IsOpen` 延迟到 `Closed` 后执行；`IsVisible=false→Collapsed` 经 BooleanToVisibilityConverter 绑定；`ClickAction` 异常由 Host 记日志 + 气泡一次不静默。
  - **M3 祛强引用 / 铁律（§2.1）**：托盘宿主代码不再引用四模块类名与业务概念词、"耳机/扬声器/空闲档位"等业务项全部迁入模块 `GetTrayMenuItems()`（ScreenLock 锁屏/空闲档位/暂停计时、TaskScheduler 任务子菜单、AudioSwitch 音频输出设备子菜单 + `DevicesChanged` 订阅、AppAutoMute 启用+设置）；宿主区仅保留 开机自启/配置编辑器/打开配置目录/重载配置/语言/退出。
  - **验证**：`dotnet build -c Debug` 0 错误 0 警告；`dotnet test` 6/6 通过；进程冷启动 4 秒存活无崩溃。

- 23:2x M10 闲时业务状态机迁纯 `IIdleService`（祛除共享 `IdleDetector` 的模块耦合） — `src/Host/Services/SystemIdleService.cs` `src/Services/Tasks/Triggers/IdleTrigger.cs` `src/Services/Tasks/TaskSchedulerService.cs` `src/Modules/ScreenLock/ScreenLockModule.cs` `src/App.xaml.cs` `src/Views/ConfigEditorWindow.xaml.cs`：
  - **`SystemIdleService` 懒启动**：事件改显式 `add/remove`，首个 `IdleTick/UserActiveDetected/SystemBusyChanged` 订阅者出现自动 `Start`，全部取消自动 `Stop`，无消费者不占 Timer。
  - **`IdleTrigger`/`TaskSchedulerService` 改消费 `IIdleService`**：构造依赖 `IdleDetector` → `IIdleService`，`IdleTrigger` 删除"全局阈值订阅 + 私有 Detector 回退"两种旧分支，改为按任务 `afterMinutes` 独立判定广播的 `RawIdle`、`UserActiveDetected` 后自动重新武装。
  - **`ScreenLockModule` 内建闲时状态机**：删除 `IdleDetector` 字段与 `Idle` 属性，订阅 `IIdleService.IdleTick`（线程池广播）复刻 `IdleDetector.OnTick` 语义（`_effectiveMs` 累积 / `WarnBefore` 提前 30s 一次警告 / 阈值触发 / 暂停·会话锁定·排程白名单·`IsSystemBusyCached` 挂起）；跨线程经 `Context.Dispatcher` 封送回 UI 触发锁屏/提醒；`ShouldSuspendIdle` 用 `IsSystemBusyCached` 替代旧的静态 `IsSystemBusy()`。
  - **门面与调用收尾**：删除 `App.Idle` 静态门面、`TaskSchedulerModule` 构造参数；`ConfigEditorWindow.ApplyRuntime` 改经 `ScreenLockMod.SetIdleMinutes` 应用空闲档位。
  - **验证**：`dotnet build -c Debug` 0 错误；`dotnet test` 6/6 通过；进程冷启动 4 秒存活无崩溃。

- 23:03 主程序集命名空间迁移（机械改名）：遗留 `ScreenLock.*` → `CarroDesk.*`（`App`/`Services`/`Views`/`Models`/`Tasks`/`Triggers`/`Localization` 各命名空间、对应 `using`、XAML `x:Class`/`clr-namespace`、`ScreenLock.App` 引用全量改名）。保留合法的 `CarroDesk.Modules.ScreenLock.*` 模块类型（`ScreenLockModule`/`ScreenLockConfig`）与成员名；不改任何行为逻辑。`dotnet build -c Debug` 0 错误 0 警告。

- 22:55 宿主解耦规范 v3.0 S1–S5 首批落地（照修订版规范实施，契约层/能力接口/音频模块迁移/退出守卫/配置落盘/测试工程） — `src/Core/` `src/Host/` `src/Modules/` `src/App.xaml.cs` `tests/CarroDesk.Tests/`：
  - **S1 契约层升级**：`IModule` 新增 `Version/Order/Status(ModuleStatus)` 与 `OnLanguageChanged`，签名迁移为 `Initialize(IModuleContext)`；新建 `IModuleContext`（ModuleId/Dispatcher/GetService/RequestTrayRefresh/ShowNotification，薄上下文）；`ModuleBase<TConfig>` 按 C#7.3 显式实现旧签名的思路改为 Context 装载配置 + Status 转换（Initialize 失败置 Faulted 并重抛，Start 禁止启动 Faulted）；`TrayMenuItem` 新增 `Order/ToolTip`。
  - **S1 容器补齐（@方案 A）**：`ServiceContainer` 支持同接口多注册 + `GetServices<T>/TryGetService/Dispose`，无第三方依赖。
  - **S1 装配**：`ModuleManager` 改按 `IMuleContext` 装配、Faulted 状态机、`OnLanguageChangedAll` 广播、`SafeInvoker`（含 3s 超时 `RunTimeout`）包裹生命周期回调；`App` 注入 Dispatcher/托盘重刷/通知回调并接入语言广播。
  - **S2 能力接口抽取注册**：`IAudioService/IForegroundTracker/IHotkeyService/INotificationService/IIdleService` 定义于 Core；`AudioService`/`ForegroundTracker` 实现接口并在容器按接口注册；`HotkeyService` 实现 `IHotkeyService`（按 moduleId 分组，新增 `UnregisterAll`）；新增 `DelegatedNotificationService`、`SystemIdleService`（懒启动单 Timer 扇出）；`AudioDeviceItem` 迁至 `Core.Models`。
  - **S3 模块迁移（AudioSwitch/AppAutoMute）**：构造取消具体服务依赖，改在 `OnStart` 经 `Context.GetService<IAudioService/IHotkeyService/IForegroundTracker>` 解析；热键迁 `IHotkeyService`（moduleId 分组，`OnStop` 回收）；`HotkeyTrigger` 改用 moduleId 分组注册。
  - **S4 配置落盘 + 退出守卫**：`ConfigService` 新增 `AudioSwitch/AppAutoMute` 两个 JSON 槽随 `config.json` 落盘，`ConfigManager` 完成两模块配置序列化/反序列化（修复此前仅存内存不落盘问题）；`ScreenLockModule` 实现 `IExitGuard`（有 PIN 则拦），`App.PromptExit` 走多守卫 3s 超时协商、挑战 UI 归 Host。
  - **S5 测试工程**：新建 `tests/CarroDesk.Tests`（net48 + MSTest），覆盖状态机枚举/托盘模型/SafeInvoker 超时与异常/容器多注册，`dotnet test` 6/6 通过。
  - **验证**：Debug/Release 双模式编译 0 警告 0 错误；应用冷启动无崩溃；渲染脚本捕获 ConfigEditor/TaskEditor 等窗口截图 UI 未错乱。
  - **按风险延后（defer，非异常）**：①命名空间全局改名 M11（与 `CarroDesk.Modules.ScreenLock` 冲突且零功能收益，留待单独批次）；② `IdleDetector` 业务状态机迁纯 `IIdleService`（M10，锁屏暂停/白名单/共享阈值精密，改动风险高，暂保留共享 IdleDetector 保证功能不变）；③托盘动态注入引擎与状态卡删除（§4/§4.4）；④三处文档修订已在上一批 22:35 完成。

- 22:35 宿主解耦规范 v3.0 实施前复核修订（对照真实代码核验诊断并补齐 5 处 spec 级问题） — `docs/HOST-PLUGIN-DECOUPLING-SPEC-v3.md`：
  - **修正 §3.1 兼容措辞**：明确 C# 7.3/net48 无接口默认实现（DIM），改为 `ModuleBase` 显式实现旧签名并在内部手工构建只读 `IModuleContext` 转发，杜绝照字面实现出错。
  - **补齐 §3.6 合并写刷盘口径**：300ms 合并写必须保模块可感知语义（读回一致/Reload 即时生效），退出前经 OnExit/Dispose Flush 最后一批，严禁退即丢写。
  - **澄清 Core 纯净性表述**：“无第三方依赖”明确为“无 NuGet 第三方”，允许依赖 WPF 基类程序集（PresentationCore/WindowsBase）。
  - **§4.1 强化对照图前置**：对照图须落盘 `temp/tray-menu-mapping-对照图.md`；标注 XAML 行号仅作基线已漂移，Step4 以对照图为准。
  - **扩 M4 / 新增 M13**：补 `App` 残留业务方法（PauseFor/SetIdleMinutes/ResumeIdle、UpdateTrayText 四态聚合、ShowBalloon）归属。
  - **§7/§8 立项测试工程**：`tests/CarroDesk.Tests`（net48、dotnet test 可跑、不含 UI），覆盖状态机/守卫协商+超时/托盘排序空锚点/老配置迁移/Idle 扇出/合并写刷盘。
  - 以上修改已按规则先备份原文件至 `temp/backups/HOST-PLUGIN-DECOUPLING-SPEC-v3.md.20260916_223132.bak`。

- 22:15 宿主解耦规范 v3.0 修订版落地（综合两份架构审查，降级 v2.0 为草案） — `docs/HOST-PLUGIN-DECOUPLING-SPEC-v3.md`：
  - **决策更新（22:25）**：§3.5 退出守卫超时锁定 3s；§4.4 状态卡决策为直接砍掉（暂停等状态并入模块菜单项表达）；§3.8 决策为手写补齐容器（MEDI 10.x 虽兼容 net48，但增重约 200–300KB 且需绑定重定向，当前约 10 个服务用 60 行补齐更划算，服务超约 20 个再评估切 MEDI 8.x LTS）。
  - **定调内置模块化单体**：废弃“插件化”措辞与 `IPlugin/TrayMenuNode` 平行体系，改为 `IModule/TrayMenuItem` 原地升级；外部 `plugins/*.dll` 明确暂不支持（net48 无 ALC、文件锁定、无隔离）。
  - **契约修正**：`IModuleContext` 薄上下文、`IExitGuard` 多播守卫替代单例伪抽象（Host PIN 兜底防安全回退）、`IIdleService` 单 Timer 扇出 + 线程契约、`IStatusProvider` 解状态卡自相矛盾、配置兼容迁移 + 原子写、I18n 广播缺口闭环。
  - **托盘与容错**：空锚点折叠、Children 变更=整区重建（删“增量”承诺）、按模块登记释放防泄漏、Faulted 状态机 + SafeInvoke 全回调包裹、禁钩子终结器；附 M1-M12 迁移清单与绞杀者路线图（每步可编译）。

# 2026-09-16

- 21:15 音频切换 COM 兼容性与热键解析修复、后台智能静音可视化配置界面上线 — `src/Core/Audio/` / `src/Models/` / `src/Host/Services/` / `src/Modules/` / `src/Views/` / `src/Assets/Locales/`：
  - **修复 Core Audio 底层切换失效缺陷**：将 `PolicyConfigClient` 的 COM CLSID 更新为 Windows 10/11 现代 GUID `{870AF99C-171D-4F9E-AF0D-E63DF40C2BC9}` 并提供工厂回退支持，同时补充 `eCommunications` 角色，端点切换在实机测试中 100% 成功生效（HRESULT=0）。
  - **增强全局快捷键符号解析**：扩展 `HotkeyHelper.TryParse`，支持反引号 `` ` `` / `~` (VK_OEM_3) 以及常用 OEM 标点符号按键与别名解析，解决 `Ctrl+\`` 快捷键注册失败的问题。
  - **加固音频设备轮转与托盘子菜单**：`AudioSwitchModule` 引入多端点 Round-Robin 兜底轮转切换机制；托盘右键菜单新增“音频输出设备”二级子菜单，动态列出当前系统所有声卡设备，标亮当前默认设备并支持即点即切。
  - **全新后台智能静音配置界面 (`AppAutoMuteSettingsWindow`)**：采用统一现代轻阴影微卡片风格，支持黑白名单双模式切换、毫秒级延迟防抖滑动微调、手动添加目标应用，并可自动扫描当前系统正在发声的活跃音频会话进程一键添加，已通过实机离屏渲染走查。

- 19:21 原生 Core Audio 音频通道一键切换与应用前后台智能静音两大新模块上线 — `src/Core/Audio/` / `src/Host/Services/` / `src/Modules/AudioSwitch/` / `src/Modules/AppAutoMute/` / `src/Views/TrayContextMenu.xaml` / `src/App.xaml.cs` / `src/Assets/Locales/`：
  - **彻底告别外部 AHK 与 nircmd 依赖**：将原 `SwitchAudio.ahk` 与 `MuteApps.ahk` 重构为 .NET 原生内存级 Core Audio WASAPI 方案，切换与静音控制耗时从数百毫秒降至毫秒级，无子进程创建开销与托盘残留。
  - **音频通道快速切换模块 (`AudioSwitchModule`)**：
    - 支持默认全局热键 `Ctrl+\`` 一键在扬声器与耳机间毫秒级切换；
    - 切换成功后自动触发轻量系统提示音，并在托盘菜单上动态更新当前输出设备标识（如 `🎧 耳机 (Realtek High Definition Audio)` / `🔊 扬声器`）；
    - 增加基于友好名称的容错模糊匹配与 `IPolicyConfig` SEH 防御保护。
  - **前后台应用智能静音模块 (`AppAutoMuteModule`)**：
    - 基于 Win32 `SetWinEventHook(EVENT_SYSTEM_FOREGROUND)` 的轻量单例 `ForegroundTracker`，委托常驻强引用杜绝 GC 回收闪退；
    - 精准防抖状态机：前台切入目标应用延迟 500ms 取消静音，切出目标应用延迟 1000ms 静音；
    - **核心安全性保障**：热键切换、模块停用或应用退出时，强制全量解除目标应用静音状态，杜绝用户软件意外处于永久静音。
  - **原有功能视口走查截图验证**：通过无感离屏视口渲染自动化捕获并走查了 `FirstRunWindow`、`VerifyPinWindow`、`ConfigEditorWindow`、`TaskEditorWindow` 全部 4 组实机窗口渲染截图（位于 `temp/screenshots/`），原有锁屏、配置管理与任务计划功能 100% 正常无损。

- 19:10 CarroDesk 模块化桌面宿主底座构建与 ScreenLock / TaskRunner 模块化重构集成上线 — `src/Core/` / `src/Host/` / `src/Modules/` / `src/CarroDesk.csproj` / `CarroDesk.slnx` / `Directory.Build.props` / `src/App.xaml.cs` / `src/Services/Tasks/TemplateExpander.cs`：
  - **工程更名与单文件独立产出**：正式将工程由 `ScreenLock` 升级命名为 `CarroDesk`，产出 `CarroDesk.slnx` 与 `src/CarroDesk.csproj`，构建生成 `CarroDesk.exe`，保持 Costura.Fody 单文件绿色便携无依赖（Release 体积约 812 KB）。
  - **恢复构建基线与模板展开支持**：依据设计规范严格补齐历史缺失的 `TemplateExpander.cs`，完整支持 `{{date}}`、`{{time}}`、`{{datetime}}`、`{{task}}`、`{{scripts}}`、`{{logs}}`、`{{dir}}` 动态模板展开与环境变量回退，构建基线恢复为 0 错误 0 警告。
  - **模块化微内核宿主底座（In-Process Modular Monolith）**：
    - **Core 契约层**：建立标准抽象契约 `IModule`、泛型抽象基类 `ModuleBase<TConfig>` 以及支持 `INotifyPropertyChanged` 的响应式托盘模型 `TrayMenuItem`；
    - **Host 基础设施层**：构建轻量级无第三方依赖的 `ServiceContainer`（依赖注入）、`ModuleManager`（模块生命周期集中调度与故障隔离）、`ConfigManager`（分区配置存取与无损平滑迁移）、`DefaultLoggerService`（日志服务）与 `App` 顶级三层未捕获异常防御网；
    - **业务模块封装**：原有的锁屏与闲时逻辑收敛封装为自治的 `ScreenLockModule`；任务自动化逻辑收敛封装为自治的 `TaskSchedulerModule`；对存量 Views 与向导维持无缝平滑兼容；
  - **测试与验证**：编写自动化反射验证与进程启动守护测试脚本（`temp/verify_modules.ps1`, `temp/verify_run.ps1`），Debug / Release 双模式编译 0 警告 0 错误，进程实机冷启动、托盘初始化及优雅退出全部测试通过。

- 18:28 全模块中英文双语国际化支持与运行时即时热切换上线 — `src/Services/Localization/` / `src/Assets/Locales/` / `src/Models/AppSettings.cs` / `src/ScreenLock.csproj` / `src/Views/` / `docs/I18N-DESIGN.md`：
  - **全模块中英双语国际化覆盖**：构建轻量级高性能的 `I18nService` 与 XAML 标记扩展 `LocExtension`（`{loc:Loc Key}`），结合 C# 门面 `Loc.T(key, args)`，完成锁屏全屏（`LockWindow`）、托盘菜单及状态卡片（`TrayContextMenu`）、配置编辑器（`ConfigEditorWindow`）、任务调度编辑器（`TaskEditorWindow`）、安全验证与首次引导向导（`VerifyPinWindow`、`FirstRunWindow`）等全模块中英文词条本地化。
  - **零重启运行时动态热切换**：利用 WPF `INotifyPropertyChanged` 结合索引器属性，用户在托盘菜单或配置编辑器中切换语言（跟随系统 / 简体中文 / English）时，所有已打开窗口与托盘即时无感热切换，无需重启程序，且锁屏时钟与日期格式随当前 Culture 自动联动。
  - **单文件独立无卫星依赖**：多语言词条（`zh-CN.json`、`en-US.json`）通过 `EmbeddedResource` 编译并经 `Costura.Fody` 嵌入主 exe，单文件绿色打包无任何外部卫星程序集或子目录；同时支持可选外部 `lang/` 目录热覆盖，扩展性与可维护性兼备。
  - **抗文本膨胀自适应布局与超时保护测试套件**：关键标题与副标题启用自适应折行（`TextWrapping="Wrap"`），布局列宽自适应规整，杜绝长英文截断贴边；编写带严格进程级超时监控（`run_with_timeout.ps1`）的双语自动化渲染测试套件，产出全模块 10 幅中英文实机视口渲染截图并通过视觉走查。

- 17:37 精简托盘重载菜单文案（移除后缀冗余文件名） — `src/Views/TrayContextMenu.xaml`：
  - **文案精简与纠错**：将托盘菜单中的「`重载配置 (settings.json)`」修正并精简为「`重载配置`」（剔除错误的 settings.json 后缀）；将任务子菜单中的「`重载任务 (tasks.json)`」精简为「`重载任务`」，界面呈现更加克制专业。

- 17:35 便携模式判定逻辑重构与数据隔离改造（引入 portable.ini 显式标志，建立 {exe}/app_data 数据隔离子目录） — `src/Services/ConfigService.cs` / `src/Services/Tasks/TaskSchedulerService.cs` / `src/Views/ConfigEditorWindow.xaml.cs` / `src/Samples/portable.sample.ini` / `src/ScreenLock.csproj` / `docs/USAGE.md`：
  - **基于 `portable.ini` 的纯粹显式便携判定**：废弃原先依赖 exe 同目录是否存在 `config.json` 的模糊判定，改为严格检测 exe 同目录下是否存在 `portable.ini`（`ConfigService.IsPortableMode`）；漫游模式与便携模式界限彻底分明，杜绝新解压绿色版首次运行时误触 `%AppData%`。
  - **建立 `app_data/` 数据隔离子目录**：便携模式下的数据根目录锁定为 `{exe_dir}/app_data/`，主配置（`config.json`）、任务调度（`tasks.json`）、任务日志（`logs/`）、扩展脚本（`scripts/`）及异常日志（`log.txt`）全部存放在该子目录下，与 exe/dll 等程序二进制彻底解耦。后续版本更新升级时，用户直接解压覆盖 exe 目录，绝不会误覆盖用户的数据与脚本。
  - **开箱即用与初始化自治**：在便携模式下，首次启动自动在 `app_data/` 创建默认配置与目录，并复制样例配置供参考，完全自治且不污染系统目录。
  - **构建与样例支持**：新增 `portable.sample.ini` 模板文件并配置构建自动复制到输出目录；更新 `ConfigEditorWindow` 模式提示文本与使用文档。

- 17:12 引入 Hardcodet.NotifyIcon.Wpf 与 Costura.Fody 实现纯 WPF 原生托盘与单文件打包 — `src/ScreenLock.csproj` / `src/FodyWeavers.xml` / `src/App.xaml.cs` / `src/Views/TrayContextMenu.xaml.cs`：
  - **引入成熟开源标准库 `Hardcodet.NotifyIcon.Wpf`**：将托盘控件由 WinForms `NotifyIcon` 替换为工业级 `Hardcodet.Wpf.TaskbarNotification.TaskbarIcon`，完全由标准 Win32 消息循环原生处理托盘事件并原生挂载 WPF `ContextMenu`。
  - **彻底移除底层手工 hack**：完全清除此前为了跨框架混编手写的 Win32 `SetForegroundWindow` P/Invoke、`_dummyWindow` 伪窗口以及菜单弹出逻辑，失焦自动收起、Esc 退出、右键呼出均由 Hardcodet 原生稳健管控。
  - **引入 `Costura.Fody` 实现单文件独立分发**：配置 `FodyWeavers.xml`，在构建期间自动将 `Hardcodet.NotifyIcon.Wpf.dll` 等依赖库压缩嵌入到主执行文件 `ScreenLock.exe` 中，输出目录不散落任何外部 DLL，保持极致清爽的单文件独立运行。

- 13:35 细节与事件绑定健壮性强化（排除进程回车防重绑、脚本下拉防重绑与重入守卫、配置保存即时同步托盘微卡片） — `src/Views/ConfigEditorWindow.xaml` / `src/Views/ConfigEditorWindow.xaml.cs` / `src/Views/TaskEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml.cs`：
  - **排除进程输入框事件规范化**：将 `ExcludeInputBox` 的 `KeyDown` 事件由 C# 动态注册改为 XAML 声明式挂载，消除每次打开配置编辑器时重复叠加绑定导致回车触发多次添加的隐患。
  - **脚本快速选择下拉框防重入与防重绑**：将 `ScriptQuickBox` 的 `SelectionChanged` 事件收敛为 XAML 声明式挂载，并在加载目录脚本列表时引入 `_isUpdating` 状态守卫，避免初始化清空与重填过程中意外触发业务逻辑或事件重复叠加。
  - **配置保存即时联动托盘状态微卡片**：在 `ConfigEditorWindow` 的 `ApplyRuntime` 中补充 `App.TrayMenu?.RefreshStatus()` 调度，确保用户在配置编辑器中修改空闲时长并点击【保存并应用】后，托盘菜单顶部的实时状态微卡片与徽章即刻刷新。

- 12:42 托盘右键菜单纯 WPF 现代化重构上线（淘汰 WinForms GDI+ 灰框，支持实时状态微卡片、多屏高分屏抗锯齿与失焦自动收起） — `src/Views/TrayContextMenu.xaml` / `src/Views/TrayContextMenu.xaml.cs` / `src/App.xaml.cs`：
  - **彻底淘汰 WinForms `ContextMenuStrip`**：移除老旧 GDI+ 灰底直角菜单与相关 WinForms MenuItem 字段，消除多显示器和高 DPI 缩放下字体与边框发虚模糊的顽疾。
  - **纯 WPF `TrayContextMenu` 现代卡片渲染**：
    - 采用 8px 圆角卡片容器与 18px 柔和外阴影 (`DropShadowEffect`)，自适应屏幕工作区与多屏 DPI；
    - 顶部新增**实时状态微卡片**：包含矢量彩色呼吸灯圆点、应用主标题、当前空闲锁定/暂停时间副标题以及状态徽章（运行中 🟢、已暂停 🟡、已禁用 ⚪、已锁定 🟠），一目了然；
    - 规范化左侧 20px 统一勾选列，无论是开机自启、空闲档位（0/1/3/5/10/15/30 分钟）还是任务调度总开关，勾选时呈现清晰的现代蓝色矢量对勾 `✓`，未勾选项左侧留白整齐对齐；
    - 子菜单（空闲预设、暂停档位、任务）配备平滑展开箭头，子菜单浮层复用现代圆角微阴影卡片。
  - **失焦关闭与交互机制优化**：通过 Win32 `SetForegroundWindow` 机制让托盘呼出的 WPF ContextMenu Popup HWND 同步获得焦点，点击菜单外部任意区域或按下 Esc 均能灵敏平滑收起；再次右键点击托盘图标触发智能 Toggle 关闭。
  - **精炼收敛与功能完整对接**：托盘菜单按最新规范收敛，保留核心操作、预设档位、暂停、自启、配置编辑器、任务（含手动运行热键展示及最近记录）、打开配置目录、重载配置及安全输入 PIN 退出。
  - **自动化测试与全状态视口验证**：编写自动化测试脚本确认了属性同步与弹出机制无异常；生成了覆盖 4 种系统状态（运行中/已暂停/已禁用/已锁定）与二级展开子菜单的视口截图，Debug 编译 0 警告 0 错误。

- 12:26 修复主操作按钮悬停白底白字缺陷、精简托盘菜单、上线任务类型矢量彩色微徽章与 Cron 自然语言解析计算 — `src/App.xaml.cs` / `src/Models/TaskDefinition.cs` / `src/Views/ConfigEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml.cs`：
  - **按钮悬停白底白字缺陷根治**：定位并修复了 `ConfigEditorWindow` 和 `TaskEditorWindow` 中主操作按钮（【保存并应用】/【保存并生效】）在鼠标悬停时因通用模板覆盖触发器导致背景变为近纯白、文字白色而完全无法看清的对比度缺陷。定义了专门的 `PrimaryBtn` 样式，悬停时平滑加深为深蓝色（`#FF1558B0`），文字保持纯白高对比度，按下时变为 `#FF0D3C7A`。
  - **托盘菜单精简收敛**：移除直接打开 tasks.json 文件的裸编辑菜单项（改用可视化任务编辑器）；移除「打开 scripts 目录」与「打开 logs 目录」，统一收敛为「打开配置目录」，使托盘主菜单和任务子菜单保持紧凑克制。
  - **任务类型微型徽章（Trigger Badges）彩色化**：在任务列表中引入彩色圆角微型标签，直观标识任务触发机制（启动/间隔/定时/Cron/锁屏/解锁/空闲/手动/热键/监听），每个类型搭配柔和浅色背景与深色字色，配合原生状态灯圆点，界面现代感显著提升。
  - **Cron 表达式自然语言翻译与下次执行时间实时计算**：`CronHelper` 新增 `ExplainCron` 与 `GetNextOccurrence`。在任务编辑器中输入或加载 Cron 表达式时，下方实时以绿字显示中文解析说明与下次精确触发时间（如 `✓ 每天 02:00 执行 · 下次: 2026-09-17 02:00`），格式非法时以红字提示具体错误原因。
  - **网格对齐与排版微调**：统一触发参数面板与主表单的 80px 网格基线，根治此前 Cron 与定时参数框错位右缩 80px 的对齐缺陷；将微型辅助字号从 10px 提升为 11px，消除 Windows 字体子像素渲染发糊和笔画粘连。
  - **视口截图与测试验证**：编写专用脚本验证了各种常见 Cron 表达式的解析与计算，并生成了最新视口渲染截图进行视觉走查，Debug 构建 0 警告 0 错误。

- 12:10 验证 PIN 弹窗、首次设置 PIN 窗口与全屏锁屏 UI/UX 全面升级 — `src/Views/VerifyPinWindow.xaml` / `src/Views/VerifyPinWindow.xaml.cs` / `src/Views/FirstRunWindow.xaml` / `src/Views/FirstRunWindow.xaml.cs` / `src/Views/LockWindow.xaml` / `src/Views/LockWindow.xaml.cs`：
  - **验证 PIN 与退出弹窗 (`VerifyPinWindow`) 现代卡片化**：
    - 废弃老旧的 Windows 7 `ToolWindow` 灰底样式，重构为现代双层卡片布局（`420px` 自适应高度，白底圆角细边框）；
    - 顶部集成品牌蓝色盾牌安全图标、加粗主标题与操作说明；
    - 输入框改用圆角居中密码框，增加大写锁定 (`Caps Lock`) 状态感知实时黄色提示；
    - 密码输错时新增平滑左右弹性抖动反馈 (`ShakeCard`) 与友好的红字提示；
    - 按钮重构为现代圆角扁平风格（【取消】浅灰边框、【确定】深蓝强调高亮），支持回车直接提交、Esc 快速取消。
  - **首次设置 PIN 窗口 (`FirstRunWindow`) 向导化重排**：
    - 重塑为精致的向导卡片风格，顶部增加安全引导徽标；
    - 输入框带有独立字段标题与必填星号标注，同样支持 CapsLock 监听与错误弹簧抖动；
    - 支持作为“首次设置”或从配置编辑器呼出的“修改解锁 PIN”窗口复用。
  - **全屏锁屏界面 (`LockWindow`) 质感与多屏增强**：
    - **主屏卡片质感**：锁屏输入卡片新增深度为 36px 的微投影悬浮阴影 (`DropShadowEffect`)，卡片顶部加入精致的深色冰蓝锁形徽标；
    - **按钮与输入框交互**：解锁按钮采用无系统灰色覆写的平滑高亮悬停效果，密码框采用 6px 圆角容器；
    - **副屏对比度修复**：解决此前副屏文字颜色过于接近背景导致几乎不可读的严重对比度缺陷，将副屏时钟提升为优雅清晰的 `#C9D1D9`，日期与状态提升为 `#8B949E`，并增加副屏锁屏标识。
  - **构建与渲染验证**：`dotnet build -c Debug` 0 警告 0 错误；自动化渲染生成并目测核验了 4 个视口截图。

- 12:05 任务编辑器草稿污染修复、事务性校验前置与状态灯矢量圆点重构 — `src/Models/TaskDefinition.cs` / `src/Views/TaskEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml.cs`：
  - **草稿污染根因修复与事务性提交**：彻底废弃此前对未通过校验对象直接追加至 `_tasks` 列表的不安全做法。实施标准的「校验前置与事务提交（Validate First, Commit Later）」模式：用户输入未通过校验（如缺少执行文件、名称重复、格式非法等）时，左侧任务列表保持完全干净，绝不塞入半成品草稿；右侧表单保留用户已输入内容供修正，并将光标精确聚焦到出错的输入控件；
  - **合法可用新建模板**：点击【新增】时自动分配全局唯一递增名称（`new-task-N`），并提供完整合法的默认模板（`startup`, 5s, `hello.js`），保证任务列表在任何时候内存与磁盘数据的一致性；
  - **状态灯矢量圆点化**：针对 Windows 默认字体下 Emoji（🟢/⚪）缺少字形回退导致渲染为方块乱码 `▯` 的痛点，`TaskDefinition` 新增 `StatusColor` 属性（`#10B981` / `#9CA3AF`），前端采用 WPF 原生 `<Ellipse>` 矢量圆点渲染，彻底根治跨系统版本豆腐块缺字问题；
  - **自动化测试与渲染验证**：通过单元测试脚本验证了校验阻断、不污染列表、焦点切换与状态灯颜色逻辑，并通过自动化视口渲染确认界面清爽规范，构建通过（Debug 0 错误 0 警告）。

- 11:55 任务编辑器右侧表单与滚动条全量交互解禁修复 — `src/Views/TaskEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml.cs`：
  - **交互解禁根因修复**：移除此前在 `ClearForm()` 中误设置的 `DetailScrollViewer.IsEnabled = false` 与提示条。WPF 中禁用 ScrollViewer 会连带锁死内部全部 TextBox、ComboBox、Button 以及滚动条事件。解禁后无论是否选中任务，右侧表单与滚动条始终保持可交互、可滚动、可即时输入；
  - **自动保存/创建联动**：在 `SaveCurrentToTask()` 中增加未选列表项时的表单自动创建逻辑，用户直接在右侧填写后点击“保存”或“测试运行”即可自动新增任务并选中，体验更加自然；
  - **触发器默认选中**：新建/清空时触发器类型默认定位至第一项（startup），相关参数面板即时展开，避免界面出现空白断层；
  - **视口截图确认**：重新运行 `temp/render_windows.ps1` 渲染输出 `temp/screenshots/preview_task.png`，视觉确认右侧全量输入控件与 6px 滚动条全部处于活跃可用状态。

- 11:50 按钮 Padding 模板绑定修复与自动化渲染截图验证 — `src/Views/ConfigEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml`：
  - **Button Padding 根因修复**：在 Button 的 `ControlTemplate` 中，`<Border>` 补充绑定 `Padding="{TemplateBinding Padding}"`，并在 `<ContentPresenter>` 中添加 `RecognizesAccessKey="True"`，彻底解决自定义模板吞掉按钮 Padding 导致文字贴边的问题。
  - **布局高度精细微调**：`ConfigEditorWindow` 默认高度调整为 700px 并收敛卡片内边距，使全部 5 个设置卡片（含最底部的 PIN 安全设置）首次打开即完整呈现；
  - **自动化渲染截图验证**：通过 `temp/render_windows.ps1` 渲染窗口精确视口并输出截图至 `temp/screenshots/preview_config.png` 与 `temp/screenshots/preview_task.png`，视觉确认按钮内边距呼吸感与 6px 贴边极简滚动条呈现理想。

- 11:43 滚动条重构（6px 极简悬浮贴边）与两编辑界面现代精致化重排 — `src/Views/ConfigEditorWindow.xaml` / `src/Views/ConfigEditorWindow.xaml.cs` / `src/Views/TaskEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml.cs`：
  - **滚动条根治与贴边重构**：
    - 解决 WPF Aero 主题系统级 `MinWidth=17` 导致滚动条无法收窄的缺陷，显式设置 `MinWidth=0` / `MaxWidth=6` / `Width=6` 与 `OverridesDefaultStyle=True`，重构为 6px 半透明动态悬浮滑块，支持 Hover（0.8 不透明度）与 Dragging（1.0 不透明度）动态反馈，轨道背景完全透明；
    - 重塑窗口流式布局：外层移除横向 Margin，`ScrollViewer` 铺满贴紧右侧边缘，内层设置留白 Padding；底部操作栏抽离为固定底栏，彻底根治滚动条“悬空截断、粗胖违和”的视觉问题。
  - **配置编辑器 (`ConfigEditorWindow`) 优化**：
    - 顶部配置文件路径收拢为紧凑微型卡片，并增加「打开目录」快捷按钮；
    - 遮罩透明度 Slider 显示百分比与状态文案（如 `88% (微透)`）；
    - 优化卡片内边距收敛空间，确保默认高度下底部的安全 PIN 卡片完整可见；
    - 排除进程列表支持双击项直接快捷删除。
  - **任务编辑器 (`TaskEditorWindow`) 优化**：
    - 任务列表增加空状态友好占位（无任务时居中展示占位指引）；
    - 增加未选中状态保护（Selection Guard）：未选择任务时右侧表单整体禁用并展示横幅引导提示，杜绝误操作；
    - 彻底规整执行 (Action) 区域网格排版，标准化文件、参数、工作目录对齐，解决原来 hack 换行导致的文字遮挡与参差不齐；
    - 选项 (Options) 超时与重试输入框收紧对齐，紧贴单位标签。
  - **现代扁平控件风格**：
    - 全面替换系统经典灰底黑框老旧按钮，统一采用现代圆角细边框扁平风格；主操作按钮采用强调蓝底白字。
  - **验证**：`dotnet build -c Debug` 与 `Release` 均成功构建（0 警告，0 错误）。

- 11:30 核心 Bug 修复、锁屏 UI/UX 动效升级与配置/任务编辑器体验增强 — `src/Services/IdleDetector.cs` / `src/App.xaml.cs` / `src/Services/KeyboardBlocker.cs` / `src/Views/LockWindow.xaml` / `src/Views/ConfigEditorWindow.xaml` / `src/Views/TaskEditorWindow.xaml`：
  - **核心 Bug 修复（Windows 会话联动 & 解锁后重复锁屏）**：
    - **Windows 解锁联动**：解决 Windows 锁屏解锁后 ScreenLock 仍需重复解锁的痛点。`AppSettings` 和 `ConfigService` 新增 `UnlockOnResume`（默认 `true`），`App.xaml.cs` 在 `SessionUnlock` 时联动调用 `Controller.Unlock()`。
    - **空闲检测秒锁修复**：重构 `IdleDetector.cs`，`Reset()` 显式清空累积毫秒并同步最新 `GetLastInputTime()`；`OnTick` 增加近期物理输入保底重置 `(raw < IntervalMs * 2)`，彻底消除了系统恢复时由于旧输入时间戳引发的误触锁屏问题。
    - **Alt+F4 逃逸漏洞修复**：`KeyboardBlocker.cs` 将原错误的 `VK_F4 = 0x70`（实际对应 VK_F1）更正为 `0x73`，并在底层钩子中拦截所有 Alt 组合键；`LockWindow.xaml.cs` 的 `OnClosing` 增加非安全授权关闭拦截。
    - **定时任务防重复触发**：修复 `DailyTrigger.cs` 构造函数，若当前时间晚于预设时间，初始化 `_lastFiredDate = now.Date`，避免启动或重载时误跑过去时刻的任务。
    - **解耦重构**：移除 `ConfigEditorWindow` 和 `TaskSchedulerService` 中的反射调用，将 `App` 内托盘与弹窗相关方法改为 `internal` 规范直调。
  - **锁屏 UI/UX 动效与时钟重构 (`LockWindow`)**：
    - **视觉与时钟布局**：重新排版锁屏界面，主屏与副屏均集成 56px 现代大数字时钟与本地化星期/日期显示；卡片采用半透明磨砂圆角微投影样式。
    - **交互动画**：新增 200ms `Opacity` 淡入和 150ms 解锁淡出过渡动画；输错 PIN 码时触发 300ms 左右弹簧抖动反馈 (`ShakeCard`)。
    - **键盘状态感知**：实时监听大写锁定 (`CapsLock`) 与数字键盘 (`NumLock`) 状态并在卡片上方动态提示，大幅降低误输率。
    - **多屏热插拔感知**：`LockController` 监听 `SystemEvents.DisplaySettingsChanged`，屏幕分辨率或显示器拔插时自动安全重建全屏蒙版。
  - **配置编辑器与任务编辑器体验增强**：
    - **配置编辑器 (`ConfigEditorWindow`)**：新增 `UnlockOnResume` 开关配置；排除进程增加“从运行中选择...”前台进程一键勾选对话框；新增“恢复默认值”功能。
    - **任务编辑器 (`TaskEditorWindow`)**：任务列表增加状态灯（🟢/⚪）与触发类型徽标；Cron 表达式编辑支持实时自然语言解析提示；参数输入框支持模板快捷下拉插入 (`{{date}}`, `{{time}}`, `{{task}}` 等)；底部增加“测试运行 (Test)”按钮支持单任务即时试跑与状态回显。
    - **托盘菜单规范化**：将“Reload Config”英文字符串标准化为中文“重载配置 (settings.json)”，菜单布局与分组规范化。
  - **验证**：`dotnet build -c Debug` 和 `dotnet build -c Release` 均 0 警告 0 错误；已完成代码静态语法与构建验证。
