# 2026-09-17

- 11:15 托盘菜单按 Module 组织规范落地（二级聚合收敛重构） — `src/Modules/ScreenLock/ScreenLockModule.cs` `src/Modules/AppAutoMute/AppAutoMuteModule.cs` `src/Modules/TaskScheduler/TaskSchedulerModule.cs` `src/Services/LockController.cs` `src/Assets/Locales/*.json` `tests/CarroDesk.Tests/CarroDeskContractTests.cs`：
  - **核心重构**：贯彻“一模块一二级子菜单（One Module, One Submenu）”规范，彻底消除此前部分模块平铺散落（立即锁屏、自动静音开关等）导致的托盘主菜单视觉割裂与过高问题，主菜单由 14 行精简至 9 行。
  - **模块收敛与状态摘要**：
    - `ScreenLockModule`：收拢散落项至单一根项 `screenlock_root`，Header 动态呈现 `🔒 屏幕保护 ({状态})`（如 `5分钟` / `暂停中` / `已禁用`），二级菜单内置顶立即锁屏一键动作；
    - `AppAutoMuteModule`：收拢开关与设置至单一根项 `appautomute_root`，Header 动态呈现 `🔇 应用后台静音 ({状态})`（`已启用` / `已禁用`）；
    - `TaskSchedulerModule`：统一 Header 格式为 `⏰ 自动化任务 ({状态})`（`运行中` / `已暂停`）；
    - 各模块均支持 `OnLanguageChanged` 与状态变更即时刷新。
  - **防御与单测覆盖**：修复 `LockController.ApplyPinFromConfig` 空配置防御；新增 4 模块二级聚合结构契约单测，`dotnet test` 11/11 全量通过。


  - **问题修复**：解决此前在主托盘菜单直接拼接长设备全名（如 `音频输出设备 (🔊 扬声器 (Realtek High Definition Audio))`）导致托盘主菜单被强行撑宽至近 400px 的视觉失衡问题。
  - **智能短名策略**：`AudioSwitchModule` 引入 `GetDeviceShortName`，精准提取耳机/扬声器核心类别（或过滤声卡硬件/芯片/驱动冗余后缀并限制最长 8 字符）；主菜单标题精简为 `音频输出设备 (🔊 扬声器)` / `音频输出设备 (🎧 耳机)`，宽度由 40+ 字符缩减为 11 字符。
  - **悬浮提示补充**：将完整设备硬件名称（`当前输出设备: {icon} {devName}`）挂载至根菜单项 `ToolTip`，实现主菜单紧凑清爽与详细设备信息兼备。
  - **多语言与生命周期同步**：标题接入 `Loc.T` 国际化，重写 `OnLanguageChanged` 确保语言即时刷新。
  - **测试覆盖**：在 `CarroDeskContractTests` 中补充扬声器/耳机/英文设备/长名称截断/空安全等多种边界的提取单测，`dotnet test` 7/7 全部通过。
