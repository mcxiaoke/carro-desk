# 2026-09-17

- 14:48 引入 Newtonsoft.Json (13.0.4) 彻底重构项目 JSON 读写基础设施 — `src/CarroDesk.csproj` `src/Services/ConfigService.cs` `src/Host/Services/ConfigManager.cs` `src/Services/Tasks/TaskConfigService.cs` `src/Services/Localization/I18nService.cs` `src/Models/*` `tests/CarroDesk.Tests/*`：
  - **核心重构与依赖升级**：引入官方最新稳定版 `Newtonsoft.Json` (13.0.4)，彻底移除原手写 `src/Extras/SimpleJSON/` 源码库（近 90KB）及内嵌的手写词法分析器。
  - **配置读写健壮化与格式规范**：`ConfigService` 与 `TaskConfigService` 淘汰 `StringBuilder` 手写字符拼接与不完整 `Escape`，改用标准缩进格式化输出；编写 `StringOrStringListConverter` 完美兼容进程过滤列表的数组与逗号分隔字符串两种形态。
  - **原生注释与容错支持**：利用 `JsonTextReader` 与 `CommentHandling.Ignore` 原生跳过 `tasks.json` / `config.json` 中的 `//` 与 `/* */` 注释，淘汰容易误伤字符串内容的正则剥离逻辑。
  - **模块配置泛型化**：`ConfigManager` 移除超过 200 行的手写字段映射代码，统一由强类型 POCO 序列化驱动。
  - **多语言扁平化精简**：`I18nService` 改用 `JObject` 递归打平，代码量缩减 50%。
  - **单文件打包与测试**：与 `Costura.Fody` 完美集成，Release 构建保持单文件独立运行（1.17MB）；新增 `JsonMigrationTests` 专项测试，全局 23 项单元测试 100% 通过。

- 12:03 桌面控制面板引入“固定位置”与“停留桌面”双状态控制 — `src/Views/FloatingPanelWindow.*` `src/Models/AppSettings.cs` `src/Assets/Locales/*.json` `tests/CarroDesk.Tests/CarroDeskContractTests.cs`：
  - **清晰职责解耦**：彻底剥离“坐标防误拖”与“失焦防收起”两项语义，顶栏右侧新增 `🔒`/`🔓` 锁定位置按钮，保留 `📌` 停留桌面按钮。
  - **固定位置 (🔒/🔓)**：已锁定时标题栏光标恢复为常规指针 `Arrow` 且完全忽略拖拽，杜绝日常点击操作误触坐标位移；未锁定时恢复 `SizeAll` 自由拖动；状态随配置持久化。
  - **停留桌面 (📌)**：强化悬浮面板多语言 Tooltip 表达，高亮时保持常驻桌面，灰色时失焦自动轻量收回。
  - **单测与构建**：配置项新增克隆与默认值校验测试，`dotnet build` 0 警告 0 错误，全局 17 项测试 100% 通过。

- 11:55 桌面悬浮控制面板交互体验优化（手柄拖拽与子菜单 Hover 即时展开） — `src/Views/FloatingPanelWindow.xaml.cs`：
  - **标题栏拖拽修复**：移除与原生非客户区冲突的 `CaptureMouse`，改为纯粹可靠的 `DragMove()` 配合按钮点击过滤，按住标题栏任意位置可自由拖拽并在松开后自动记忆为 `Custom` 坐标。
  - **子菜单 Hover 自动滑出**：实现 `AttachHoverBehavior`，鼠标滑过带有子菜单的 MenuItem 时自动展开二级/三级子菜单（`IsSubmenuOpen = true`）并自动关闭同级其他已展开项，彻底消除需额外点击一次的阻滞感。
  - **构建与测试**：消除未引用私有字段警告，`dotnet build` 0 警告 0 错误，全局 17 项单元测试全量通过。

- 11:49 引入桌面悬浮控制面板（FloatingPanelWindow） — `src/Views/FloatingPanelWindow.*` `src/Views/TrayContextMenu.*` `src/Models/AppSettings.cs` `src/Services/ConfigService.cs` `src/App.xaml.cs` `src/Assets/Locales/*.json` `tests/CarroDesk.Tests/CarroDeskContractTests.cs`：
  - **核心能力**：打造现代轻量微卡片桌面悬浮控制中心，彻底打通托盘数据模型复用；支持双通道唤出（全局快捷键 `Win+Alt+C` 或双击托盘图标）。
  - **停留桌面与多模定位**：
    - 提供顶部控制栏与自由拖拽手柄（`DragMove`），松开自动记录最新坐标；
    - **📌 钉住功能（Pin）**：钉住状态下失焦不关闭，支持连续操作；未钉住状态下失焦自动轻量收回；
    - **📍 位置策略**：内置“靠任务栏托盘（默认）”、“屏幕中央”、“屏幕右上角”、“记忆当前位置”四种自适应对齐模式，配置自动落盘持久化。
  - **全量业务复用**：直接基于 `IModule.GetTrayMenuItems()` 与宿主通用项渲染，业务动作、状态同步与多语言 100% 原生复用。
  - **单测全量通过**：补充配置读写与克隆契约单测，全局 17/17 项测试全部通过。


  - **模块化迁移落地**：将 MonitorTray 的显示器控制与多情境调度能力重构为原生业务模块 `MonitorProfileModule`（继承 `ModuleBase<MonitorProfileConfig>`，遵循 CarroDesk 内置单体规范）。
  - **纯 Win32 + WMI 零外部依赖**：彻底移除对外部 `ddccli.exe` 进程依赖，纯托管 P/Invoke `dxva2.dll`（High-Level / Low-Level VCP 0x10/0x12）并支持 WMI 笔记本内屏自动回退；适配 C# 7.3 与 .NET Framework 4.8，保持单 EXE 便携打包特性。
  - **异步队列与并发防抖**：针对 I2C/DDC 硬件 I/O 阻塞痛点，构建后台单通道异步队列与防抖机制，杜绝主 UI 线程与托盘菜单卡顿；严格控制物理句柄生命周期杜绝 GDI/内核泄漏。
  - **智能情境调度与电源自愈**：支持日常/游戏/夜间等多情境时间表自动切换；监听 `PowerModeChanged` 与 `SessionSwitch`，设计两阶段异步延迟重连机制化解睡眠唤醒后的 DDC/CI 握手延迟。
  - **WPF 现代配置窗口与二级收敛托盘**：提供直观的现代卡片式情境编辑界面；托盘遵循“一模块一二级子菜单”规范，支持模式快捷单选、常用档位微调、计划切换与刷新动作。
  - **单测全量通过**：新增配置序列化/时间匹配/托盘契约测试，全套测试 16/16 编译并通过。

- 11:15 托盘菜单按 Module 组织规范落地（二级聚合收敛重构） — `src/Modules/ScreenLock/ScreenLockModule.cs` `src/Modules/AppAutoMute/AppAutoMuteModule.cs` `src/Modules/TaskScheduler/TaskSchedulerModule.cs` `src/Services/LockController.cs` `src/Assets/Locales/*.json` `tests/CarroDesk.Tests/CarroDeskContractTests.cs`：
  - **核心重构**：贯彻“一模块一二级子菜单（One Module, One Submenu）”规范，彻底消除此前部分模块平铺散落（立即锁屏、自动静音开关等）导致的托盘主菜单视觉割裂与过高问题，主菜单由 14 行精简至 9 行。
  - **模块收敛与状态摘要**：
    - `ScreenLockModule`：收拢散落项至单一根项 `screenlock_root`，Header 动态呈现 `🔒 屏幕保护 ({状态})`（如 `5分钟` / `暂停中` / `已禁用`），二级菜单内置顶立即锁屏一键动作；
    - `AppAutoMuteModule`：收拢开关与设置至单一根项 `appautomute_root`，Header 动态呈现 `🔇 应用后台静音 ({状态})`（`已启用` / `已禁用`）；
    - `TaskSchedulerModule`：统一 Header 格式为 `⏰ 自动化任务 ({状态})`（`运行中` / `已暂停`）；
    - 各模块均支持 `OnLanguageChanged` 与状态变更即时刷新。
  - **防御与单测覆盖**：修复 `LockController.ApplyPinFromConfig` 空配置防御；新增 4 模块二级聚合结构契约单测，`dotnet test` 11/11 全量通过。


  - **问题修复**：解决此前在主托盘菜单直接拼接长设备全名（如 `音频输出设备 (🔊 扬声器 (Realtek High Definition Audio))`）导致托盘主菜单被强行撑宽至近 400px 的视觉失衡问题。
  - **智能短名策略**：`AudioSwitchModule` 引入 `GetDeviceShortName`，精准提取耳机/扬声器核心类别（或过滤声卡硬件/芯片/驱动冗余后缀并限制最长 8 字符）；主菜单标题精简为 `音频输出设备 (🔊 扬声器)` / `音频输出设备 (🎧 耳机)`，宽度由 40+ 字符缩减为 11 字符。
  - **悬浮提示补充**：将完整设备硬件名称（`当前输出设备: {icon} {devName}`）挂载至根菜单项 `ToolTip`，实现主菜单紧凑清爽与详细设备信息兼备。
  - **多语言与生命周期同步**：标题接入 `Loc.T` 国际化，重写 `OnLanguageChanged` 确保语言即时刷新。
  - **测试覆盖**：在 `CarroDeskContractTests` 中补充扬声器/耳机/英文设备/长名称截断/空安全等多种边界的提取单测，`dotnet test` 7/7 全部通过。
