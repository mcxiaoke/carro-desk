# 变更记录

## 2026-09-24 19:25 — 全项目各窗口 UI 渲染与文字截断审查与修复

**背景**：根据实测反馈，项目各窗口（包括设置、弹窗与面板）存在部分控件因宽度不足或内边距过大导致文案被截断（如显示器亮度配置窗口左下方操作按钮“重命名”截断为“重命”、DataGrid 操作列截断为“操”等）。通过自动化渲染脚本遍历项目全部 13 个独立窗口生成屏幕截图，逐一审查排查缺陷并实施修复。

**UI 布局与截断修复**
- **显示器情境设置（MonitorProfileSettingsWindow）**：
  - 修复左下方模式操作按钮截断问题：将模式卡片列宽由 `180` 拓宽至 `205`，按钮内边距调整为 `Padding="2,4"`，保证“新建”、“重命名”、“删除”三个按钮文案完整美观且留有边距。
  - 修复 DataGrid 列宽截断问题：优化时间表各列宽度分配（`85 / 105 / 105 / *`，操作列 `MinWidth=75`），彻底消除表头“操作”被截断为“操”及行内“删除”按钮挤压问题。
  - 默认窗口尺寸调整为 `780 x 700`（`MinWidth=740, MinHeight=680`），全局快捷键区域与时间设置区域舒展无溢出。
- **音频切换设置（AudioSwitchSettingsWindow）**：
  - 修复顶部横幅文案与状态徽章撞车问题：设置说明 StackPanel 右侧增加 `Margin="0,0,12,0"` 与 `TextWrapping="Wrap"`。
  - 修复匹配关键字示例文字截断问题：调整关键字输入框列宽（`140`），使 `(默认: 耳机 / Headphones)` 完整呈现不被裁剪。
  - 默认窗口尺寸优化为 `660 x 700`（`MinWidth=640, MinHeight=680`）。
- **应用后台自动静音设置（AppAutoMuteSettingsWindow）**：
  - 修复顶部横幅文案与启用勾选框贴靠重叠问题：增加右侧边距 `Margin="0,0,16,0"`，防止长文案贴近或遮挡“启用此功能”复选框。
  - 默认窗口尺寸优化为 `660 x 720`（`MinWidth=620, MinHeight=680`）。
- **保持唤醒设置（AwakeSettingsWindow）**：
  - 修复下方智能进程联动卡片在默认分辨率下被底边裁剪的问题：微调卡片垂直间距与边距，微调进程列表框高度为 `65`，调整窗口默认高度为 `760 x 600`，使“退出缓冲时间”等全套控件在无需滚动条的情况下即可完整展示。
- **品牌名称规范化（zh-CN.json / en-US.json）**：
  - 将旧版遗留的 `ScreenLock` 全局标题与文案统一规范为 `CarroDesk`，涵盖 `FirstRunWindow` 欢迎标题、`VerifyPinWindow` 标题、`ConfigEditorWindow` 标题、`TaskEditorWindow` 标题以及托盘退出/悬浮提示。

**视觉验证与测试**：
- 重新渲染全部窗口截图并完成人工像素级核对（产物存储于 `temp/screenshots/`），所有被裁切和重叠问题全部解决。
- 全量 186 项单元测试全部通过（186/186），编译 0 警告 0 错误。

## 2026-09-24 18:25 — 架构精炼与样板代码削减（做减法：ModuleBase 下沉与宿主菜单动作统合）

**背景**：针对模块和宿主间"每次修改就堆砌样板代码"的增生问题实施整体架构精简与做减法，在保证功能可靠性、UI 渲染与托盘交互零破坏的前提下大幅精简代码量。

**基类基础设施下沉（ModuleBase）**
- **非空配置保证**：泛型配置自动初始化默认实例，消除各模块脆弱的 fallback 占位 hack。
- **声明式受控快捷键（Managed Hotkeys）**：各模块由声明热键 Getter 委托接管，启动自动绑定、配置热重载自动同步按键变化、停止与析构自动注销，杜绝事件与句柄残留。
- **通用托盘与通知服务下沉**：基类内置跨线程安全的 `SetTrayItemSelf`、`RequestTrayRefresh` 及 `ShowNotify`，统一调度与异常兜底。

**业务模块瘦身（Awake / AudioSwitch / AppAutoMute / Clipboard / MonitorProfile / ScreenLock）**
- 彻底剔除各模块重复手写的快捷键 ID 记录、注销循环、Dispatcher 线程跳板及重复通知方法，各模块代码量净削减近 500 行。

**宿主菜单交互收敛（HostMenuActions）**
- 抽象统一 `HostMenuActions`，统合开机自启切换、配置目录调用、配置编辑器拉起与全局语言切换，消除 `TrayContextMenu` 与 `FloatingPanelWindow` 的动作重复实现。
- 复用 `MenuProjectionEngine` 悬停行为并清理未使用的死代码。

**验证与指标**：
- 全局代码量净削减 200+ 行（模块业务层削减 490+ 行重复样板代码）。
- 全量 186 项单元测试持续通过，编译 0 警告 0 错误。

## 2026-09-24 17:30 — 重构保持唤醒（Awake）进程联动为独立状态并优化状态展示

**背景**：原 Awake 进程联动直接修改用户模式 `_mode = AwakeMode.Indefinite`，导致用户持久化配置被污染、托盘菜单勾选项冲突，且原有联动状态展示易混淆。此外，在用户已手动保持唤醒期间，联动进程进出会发送重复干扰通知。

**进程联动状态解耦（AwakeService）**
- 将进程联动抽离为独立叠加状态 `_isProcessTriggered`，不再篡改用户手动基底模式 `_mode` 与持久化配置文件。
- 引入 `IsProcessExiting` 状态与 `ProcessExitPendingSeconds`，使退出缓冲期状态可感知且透明可追踪。
- 优化进程枚举效率：单次采样全系统进程快照比对，替代循环枚举；修复多目标进程切换时的残留问题。
- 用户手动"关闭"后记录会话抑制，并在守护进程全部退出、手动切换模式、保存设置或重新开启联动时安全复位。
- 配置更新或名单清空时即时撤销正在进行的联动唤醒，消除状态残留。

**菜单与提示呈现优化（AwakeModule / AwakeSettingsWindow）**
- 静默联动：若用户本来就在手动保持唤醒状态（永久或定时），进程进出不弹出气泡通知，避免打扰。
- 托盘根节点与悬浮提示清晰分层：电池暂停 > 用户手动模式（永久/定时） > 进程退出缓冲（含剩余秒数） > 进程联动运行中 > 遵循系统默认关闭。
- 解决菜单勾选冲突：系统处于进程联动时，"关闭"项不再呈现勾选，仅在真正处于 Passive 且无联动时勾选。
- 托盘菜单项 `TrayMenuItem.ToolTip` 补充 `OnPropertyChanged` 通知，支持退出缓冲秒数动态刷新。
- 设置窗口状态徽章支持黄色缓冲倒计时展示，并在保存设置时复位抑制状态。

**测试**：更新并新增 Awake 进程联动隔离与退出缓冲单元测试，全量 186 项测试全部通过。

## 2026-09-24 17:15 — 修复开机自启动路径自愈、异常反馈与启动任务去重

**背景**：审查自启动处理发现便携模式移动目录后注册表残留失效路径无法自愈、可执行文件定位存在 DLL 风险、注册表写入异常被全吞导致 UI 假阳性勾选，以及计划任务模块中 startup 触发器在配置重载时被重复触发。

**自启服务（AutoStartService）**
- 使用 Process.GetCurrentProcess().MainModule.FileName 与 Assembly.GetEntryAssembly() 可靠解析主程序路径并校验磁盘存在性。
- IsEnabled() 增加路径比对：注册表键值存在但路径失效时判定为 alse。
- Sync() 与 SetEnabled() 支持路径自愈，并返回 ool 操作结果。

**界面与反馈（TrayContextMenu / FloatingPanelWindow）**
- 切换自启若被安全软件拦截或写入失败，捕获并提示气泡通知，避免配置与 UI 勾选产生假阳性。

**启动与调度（App.xaml.cs / TaskSchedulerService）**
- App.OnStartup 显式重置 Environment.CurrentDirectory = AppDomain.CurrentDomain.BaseDirectory，避免 Windows 开机拉起时工作目录偏离至 System32。
- TaskSchedulerService 增加启动任务执行记忆 _firedStartupTasks，避免托盘重载配置或保存设置时误重复触发开机延迟任务。

**i18n**：zh-CN / n-US 补充 Tray.AutoStartFailed 国际化文案。
**测试**：新增 AutoStartService_GetCurrentExecutablePath_ReturnsValidPath 单元测试，全量 184 项测试通过。
