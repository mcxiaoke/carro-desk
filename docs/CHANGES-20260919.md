# 变更记录

## 2026-09-19 17:10 — 剪贴板最多3行预览与窗口自由放大、全系统本地化契约修复与数据联动增强

- **剪贴板历史窗口交互与多行预览增强** (`ClipboardHistoryWindow.xaml` / `ClipboardItem.cs` / `ClipboardHistoryService.cs`)：
  - 正文预览从单行截断升级为最多支持 3 行展示 (`BuildPreviewText`)，换行规范化为 `\n`，消除 `\r` 带来的单测与跨平台不匹配，在列表项内支持自动折行与超出省略。
  - 窗口交互全面开放自由缩放：支持鼠标边缘拖拽 (`CanResizeWithGrip`)、标题栏按住拖动窗口、右上角一键放大/还原 (`BtnMaximize` 780x720 视口) 以及右下角现代斜线抓手 (`Thumb`) 拖动缩放，极大提升大段文字历史的查阅体验。
- **全系统中文化、本地化 Key 与兜底机制彻底修复** (`Loc.cs` / `zh-CN.json` / `en-US.json`)：
  - 修复 `Loc.cs` 缺少 `T(string key, string defaultValue)` 导致编译器重载至 `(key, params object[] args)` 的关键隐患，确保缺失 key 时 100% 返回缺省文本而非原始 key 字符串。
  - 全量扫描并补齐各模块二级托盘菜单及弹窗的缺失键值（包括 `Tray.ScreenLockSettings`、`Tray.AudioSwitchSettings`、`Tray.MonitorProfileSettings`、`Tray.OpenTaskLogsDir`、`Tray.AutoMuteUnmuteAll`、`Tray.FastToggle`、`Tray.PauseCustom` 及所有中英文字符串），补齐 `ConfigEditorWindow`、`FloatingPanelWindow`、`ClipboardHistoryWindow` 的 XAML 本地化绑定。
- **UI 与数据联动增强** (`AudioSwitchModule.cs` / `MonitorProfileModule.cs` / `TaskSchedulerModule.cs` 等)：
  - 音频切换在设备变更后自动触发托盘刷新 (`RequestRefreshSelf`)，保证快捷键切歌/切设备时托盘菜单选中态 (`IsChecked`) 与当前系统真实设备严格同步。
  - 显示器情境模式、计划开关、手动探测及任务调度的通知与菜单项全面接入双语国际化与数据变动事件响应。

## 2026-09-19 09:47 — AudioSwitch 补齐设置窗口、设备排除黑名单机制与托盘设置入口

- **新建轻量级配置窗口** (`AudioSwitchSettingsWindow.xaml`)：彻底解决音频模块此前完全没有设置 UI 的严重短板。支持可视化修改全局热键（格式校验）、提示音开关、耳机与扬声器关键字配置，并支持从当前设备列表一键提取填入关键字。
- **设备排除黑名单机制**：`AudioSwitchConfig` 增加 `ExcludedDevices` 列表，快捷键轮询切换时自动跳过黑名单设备（完美解决台式机显卡切入无声 HDMI 显示器的问题）；若所有设备均被排除，提供回退保护防止无声死锁。
- **设备管理与托盘入口闭环**：设置界面支持刷新设备、勾选排除、一键设为当前默认输出及模拟快捷切换测试；托盘二级菜单增加“音频切换设置...”入口，并对已排除设备予以直观标注。

## 2026-09-19 09:43 — ScreenLock 补齐全局热键注册、锁屏设置入口、排除进程浏览EXE与系统锁屏逃生通道

- **全局锁屏热键支持**：`ScreenLockConfig` 引入 `Hotkey` 属性（默认 `Ctrl+Alt+L`），模块完整接入 `IHotkeyService` 全局热键生命周期；托盘立即锁定项及配置中心全面同步热键配置与格式校验。
- **锁屏界面逃生通道**：`LockWindow` PIN 验证卡片下方新增“🔒 切换至 Windows 锁屏”逃生入口，遇到输入法卡死或忘记 PIN 时可一键无缝降级至 Windows 原生登录锁屏（调用 `LockWorkStation`），杜绝死锁风险。
- **排除进程浏览与清空**：配置中心排除进程维护区新增“📂 浏览 (.exe)...”按钮，支持从本地直接选取可执行文件添加；列表新增右键菜单（删除所选、清空列表）。
- **托盘菜单完善**：补齐托盘二级“屏幕保护设置...”入口，并在暂停计时子菜单新增“自定义暂停分钟数...”选项。

## 2026-09-19 09:39 — Awake 补齐进程联动浏览EXE、列表右键清空与托盘自定义时长输入

- **关联进程浏览与清空**：智能进程联动配置区新增“📂 浏览 (.exe)...”按钮，支持直接在本地磁盘选取未启动的可执行程序；关联进程列表新增右键菜单（删除所选、清空列表），提升多进程批量维护效率。
- **托盘自定义定时时长**：托盘“定时保持唤醒”子菜单新增“自定义分钟数...”输入入口，支持随时指定 1~1440 任意分钟数，无需每次进入完整设置窗体修改。

## 2026-09-19 09:37 — MonitorProfile 补齐情境模式重命名(Rename)、右键菜单与时间段双击载入就地更新

- **情境模式重命名与右键上下文**：模式列表新增“重命名”操作按钮及右键菜单项（设为当前模式、重命名模式...、删除模式），支持自定义模式名称（如“阅读模式”、“观影”），且重命名后自动迁移绑定的时间段计划与当前激活状态。
- **时间段列表双向编辑与载入更新**：`MonitorTimeSetting` 增加 `INotifyPropertyChanged` 支持；DataGrid 开放双向绑定，支持行内直接修改；点击行或双击行自动载入底部编辑栏，新增“更新选中”按钮，彻底解决原有只能删除重加的繁琐交互缺陷。

## 2026-09-19 09:35 — AppAutoMute 补齐浏览EXE添加、清空受控列表、应急恢复声音及托盘快捷操作

- **可执行文件浏览支持**：设置界面在“手动输入进程名”旁新增“📂 浏览 (.exe)...”按钮，支持从本地磁盘任意位置选取未启动的应用程序，自动解析进程名并即时添加至控制列表。
- **列表清空防护操作**：受控程序列表增加“清空列表”按钮及防误触确认对话框，方便批量清理旧配置项。
- **应急一键解静音**：设置窗口底部操作栏及系统托盘二级菜单新增“🔊 恢复所有程序声音 (应急)”入口，在进程识别异常或崩溃卡死误静音时可一键瞬间恢复所有音频会话。
- **托盘极速添加前台应用**：托盘菜单新增“➕ 将当前前台应用加入静音列表”快捷入口，无需打开设置界面即可随时捕获当前前台活动窗口应用。

## 2026-09-19 09:33 — TaskScheduler 补齐列表一键启停、脚本/日志目录与文件直达、托盘执行全任务

- **列表一键启停交互闭环**：任务列表每项新增独立启停 CheckBox 及右键菜单（立即运行、启停切换、查看日志），修改后自动存盘并即时通知调度引擎重载，无需在右侧表单逐个翻找保存。
- **脚本与日志直达**：动作编辑区新增“📂 脚本目录”按钮直达 `scripts` 目录；底部操作栏新增“📄 查看当前日志”与“📂 日志目录”按钮，支持一键用系统默认程序打开单任务专属日志或聚合日志。
- **托盘手动执行范围扩展**：托盘“手动运行任务”由原先仅支持 `trigger: "manual"` 任务扩展为支持所有当前启用的计划任务（附带 `[手动]`、`[定时]`、`[空闲]`、`[解锁]`、`[开机]` 等直观类型徽标），方便临时调试或紧急触发；托盘右键新增“打开任务日志目录...”快捷入口。

## 2026-09-19 09:30 — ClipboardHistory 补齐右键菜单、单条删除、Pin置顶、清空二次确认与真实粘贴模拟

- **单条操作闭环**：列表项新增右键菜单（复制并粘贴、仅复制、固定置顶、删除、查看详情）及条目快速删除 `✕` 与置顶 `📌` 按钮，彻底解决纯鼠标用户无法删除单条记录痛点。
- **置顶机制与防清理保护**：`ClipboardItem` 增加 `IsPinned` 支持，置顶条目稳定优先展示在列表顶端，过期与容量截断时受保护不被淘汰。
- **清空安全防误触**：窗口及托盘清空操作均增加确认弹窗，默认保留已置顶项，杜绝手滑导致全量历史丢失。
- **真实模拟自动粘贴**：解决原有文案承诺“复制并粘贴”但仅写回剪贴板的断层问题，支持写回后通过 Win32 模拟 `Ctrl+V` 自动粘贴至前台目标窗口。

## 2026-09-19 09:12 — 全面排查旧配置残留并更新全模块样例配置

- **旧配置全面排查**：确认各业务模块（`ScreenLock`、`TaskScheduler`、`ClipboardHistory`、`AudioSwitch`、`AppAutoMute`、`MonitorProfile`、`Awake`）及宿主均已全面基于独立模块化架构读写，无任何存量业务代码残留旧扁平读写逻辑。
- **全模块 Sample 配置补齐与更新** (`src/Samples/` & `Tools/`)：
  - `config.sample.json`：全面补齐 `AudioSwitch`、`AppAutoMute`、`MonitorProfile`、`Awake` 等全部 7 个模块的配置段结构、注释及合法值说明，统一模块化层次规范。
  - `portable.sample.ini` & `tasks.sample.json`：清理全部历史 `ScreenLock` 旧软件名与路径残留，统一为 `CarroDesk` 标准路径与丰富自动化触发器样例。
## 2026-09-19 09:08 — 修复 ScreenLock 锁屏透明度及旧配置迁移失效问题

- **旧配置平滑迁移与污染清理** (`ConfigService.cs`)：
  - 自动识别并迁移根级历史扁平字段（`OverlayOpacity`、`ExcludeProcesses`、`IdleMinutes`、`ShowClock`、`UnlockOnResume`）至 `ScreenLock` 模块节点，`TasksEnabled` 迁移至 `TaskScheduler` 模块节点。
  - 迁移完成后彻底清理根级遗留字段与无效空字符串节点，避免保存时回写根级造成双轨配置与配置覆盖失效。
- **透明度调节精度优化** (`ConfigEditorWindow.xaml`)：
  - `OpacitySlider` 的 `TickFrequency` 与 `SmallChange` 调整为 `0.01`（1% 精细步长），支持 30%~100% 任意百分比精确设定（如 88%、98%、100%），消除原 5% 步长自动吸附导致微调值被覆盖的问题。
- **锁屏渲染防御** (`LockWindow.xaml.cs`)：
  - 对 `OverlayOpacity` 实施 `[0.1, 1.0]` clamp 防御，避免极端异常值影响淡入动画。
- **测试覆盖** (`JsonMigrationTests.cs`)：
  - 新增针对历史扁平配置向 `ScreenLock` 与 `TaskScheduler` 迁移与根属性清理的单元测试，全量 89 项测试 100% 通过。

