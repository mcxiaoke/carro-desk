# 变更记录

## 2026-09-25 15:59 — 全面审查后首批正确性与数据安全修复

- **配置与持久化**：配置热重载改为完整校验后提交，失败时保留上一份有效配置并停止应用；模块与宿主设置保存现在返回真实结果，失败时回滚内存状态；原子写降级路径增加旧文件恢复，避免目标配置丢失。
- **任务调度**：未知任务触发器不再静默转为启动执行；任务编辑器在存在解析错误或未来版本时禁止覆盖原文件，修复合法触发器别名回显为启动任务；首次启动自动创建任务文件；Cron 增加阻塞补触发与闰日调度，条件探测改为失败关闭，禁用任务停止后续重试。
- **锁屏与 PIN**：修复未配置 PIN 时假成功、键盘钩子安装失败仍锁定、休眠后闲时漏计、已排队锁定竞态及模块停止后锁窗残留；锁屏、退出和修改 PIN 共享进程级失败限流；损坏 PIN 可进入重新设置流程。
- **音频、Awake 与剪贴板**：音频默认端点按三种角色一致性提交并在部分失败时回滚；Awake 持久化定时截止时间并修复总开关与电池暂停残留；剪贴板回写失败不再发送粘贴，粘贴前验证原目标窗口，监听事件即时捕获内容，损坏历史先备份，配置与预览规则得到防御性归一化。
- **模块与界面可靠性**：模块生命周期失败增加回滚和故障状态，受管热键注册失败保留旧键；动态托盘过滤无效模块并隔离迭代异常；显示器计划仅在硬件成功后标记已应用，关闭自动计划后不再被唤醒事件强制应用；悬浮面板支持副屏与负坐标位置恢复；保留系统级 DPI 感知以避免 Win32 物理像素与 WPF DIP 混用。
- **验证**：新增配置损坏重载、未来 schema 只读保护、未知 trigger、别名归一化、PIN 格式、剪贴板越界、Awake 状态恢复、热键和闰日 Cron 等回归测试；Release 构建 0 警告、0 错误，全量测试 195/195 通过。
