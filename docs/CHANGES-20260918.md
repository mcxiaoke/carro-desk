# 2026-09-18

- 15:25 Cron 表达式编译型位运算重构与 TaskDefinition 领域模型下沉拆分（P1-4, P1-6） — `src/Services/Tasks/CronHelper.cs` `src/Services/Tasks/HotkeyHelper.cs` `src/Models/TaskDefinition.cs` `src/Views/TaskEditorWindow.xaml.cs` `tests/CarroDesk.Tests/*`：
  - **Cron 表达式高性能编译与维度跳跃算法 (P1-4)**：新增 `CompiledCronExpression`，将 5 字段解析为 64 位/32 位掩码，以纯位运算替代运行时字符串分割；`GetNextOccurrence` 引入智能跨维度跳跃（按月、按日、按小时直接跳过无效区间），彻底消除最坏 52 万次循环与数百万临时字符串分配；在 `TaskEditorWindow` 对表达式输入框增设 300ms 防抖计时器，彻底消除按键时的卡顿。
  - **TaskDefinition 瘦身与工具类解耦下沉 (P1-6)**：将此前挤压在 `TaskDefinition.cs` 中的 `CronHelper`（约 170 行）与 `HotkeyHelper`（约 100 行）完整下沉独立为 `src/Services/Tasks/CronHelper.cs` 和 `HotkeyHelper.cs`，`TaskDefinition.cs` 缩减至纯粹的领域实体与 DTO（约 300 行）；原热键解析改为 `public` 供多模块共享。
  - **测试验证**：全量 72 项单元测试 100% 通过（含新增 500 次跨月高性能 Cron 预测基准用例）。

- 15:15 修复配置安全原子写、悬浮面板属性保护、任务并发与防抖机制（P0-5, P0-6, P0-7, P0-8） — `src/Common/AtomicFile.cs` `src/Views/ConfigEditorWindow.xaml.cs` `src/Services/ConfigService.cs` `src/Services/Tasks/TaskConfigService.cs` `src/Services/Tasks/TaskSchedulerService.cs` `src/Services/Tasks/Triggers/FileWatcherTrigger.cs` `tests/CarroDesk.Tests/*`：
  - **悬浮面板配置保护 (P0-5)**：`ConfigEditorWindow.BuildCurrent` 改为基于 `_editing.Clone()` 派生，完整保留 `FloatingPanelHotkey`、`Position`、`Pinned`、`Locked`、`X/Y` 及哈希盐，彻底修复保存配置时悬浮面板被静默重置为默认值的缺陷。
  - **配置损坏自愈与安全原子写 (P0-6)**：实现公共类 `AtomicFile.WriteAllText`（同目录临时文件 + `File.Replace` / 安全重命名回退机制）；`ConfigService` 与 `TaskConfigService` 全面接入原子写，反序列化异常时自动备份 `.corrupt-{timestamp}.json` 防止现场丢失与静默覆盖。
  - **任务并发互斥窗口消除 (P0-7)**：移除 `TaskSchedulerService.Apply` 中的 `_running.Clear()`，避免动态 Reload 冲刷正在执行的任务标记导致并发互斥失效；清理不可达的 JSON 字符串扫描代码。
  - **文件监控按路径防抖 (P0-8)**：`FileWatcherTrigger` 改用 `ConcurrentDictionary<string, DateTime>` 按文件全路径独立防抖并定期剔除过期键，彻底解决批量拖放文件时除第 1 个外后续事件全部被吞的缺陷。
  - **测试验证**：新增 `ConfigAndTriggerTests` 覆盖原子写可靠性、配置字段克隆与单/多文件并发防抖测试；全量 71 项单元测试 100% 通过。

- 15:11 修复全局未处理异常致命拦截与白名单模式前台发声异常（P0-3, P0-4） — `src/App.xaml.cs` `src/Modules/AppAutoMute/AppAutoMuteModule.cs` `tests/CarroDesk.Tests/*`：
  - **全局致命异常拦截防损坏 (P0-3)**：在 `App.OnDispatcherException` 识别 `OutOfMemoryException`、`StackOverflowException`、`AccessViolationException` 等致命状态，写入独立 `fatal.log` 后执行 `Environment.FailFast` 终止进程，杜绝托管堆损坏后继续运行破坏用户数据；普通非致命异常弹气泡提示并记录日志。
  - **白名单模式切回前台不出声根因修复 (P0-4)**：重构 `AppAutoMuteModule.EvaluateForeground`，白名单模式下任意进程切入前台均自动挂起其静音状态并重置解静音计时器；在 `OnMuteTimerTick` 执行静音后追加当前前台解静音兜底，杜绝非白名单应用（如 Chrome）在后台被静音后切回前台永久失声的缺陷；修复空 TargetApps 短路阻断白名单的问题。
  - **测试验证**：新增 `AppAutoMuteModuleTests` 覆盖致命异常识别、托盘二级菜单收敛契约与配置默认值；全量 68 项单元测试 100% 通过。

- 15:08 修复 Win32 QUNS 枚举常量错位与跨午夜亮度调度回退缺陷（P0-1, P0-2） — `src/Host/Services/SystemIdleService.cs` `src/Modules/MonitorProfile/Services/ProfileScheduleEngine.cs` `tests/CarroDesk.Tests/*`：
  - **QUNS 状态常量对齐 Win32 标准定义 (P0-1)**：更正 `SystemIdleService` 中 `QUNS_RUNNING_D3D_FULL_SCREEN`=3、`PRESENTATION_MODE`=4、`ACCEPTS_NOTIFICATIONS`=5、`QUIET_TIME`=6，彻底修复常规桌面（返回 5）被误判为 busy 导致锁屏被冻结、以及全屏 3D 游戏（返回 3）反而被判定为不忙的严重缺陷；新增 `SystemIdleServiceTests` 覆盖全部状态判定。
  - **跨午夜亮度调度回退取值修复 (P0-2)**：修复 `ProfileScheduleEngine.GetActiveSettingForProfile` 在早于当天首个档位时回退取错为升序首档的缺陷，更正为降序列表第一项（即前一天夜间最大时间点）；在 `MonitorProfileTests` 补充 00:00、06:00、07:00、18:00、22:30 等全时段跨午夜单测断言。
  - **测试验证**：全量 65 项单元测试 100% 通过。

- 14:30 补充非 UI 核心业务逻辑单元测试集与隐蔽缺陷修复 — `src/Services/PinService.cs` `src/Services/Tasks/TemplateExpander.cs` `src/Services/Tasks/TaskConditionEvaluator.cs` `tests/CarroDesk.Tests/*`：
  - **核心测试集补充覆盖**：
    - **安全与防爆破核心 (`PinServiceTests`)**：覆盖 PBKDF2 哈希运算、旧版单轮 SHA256 密码首次校验透明无感升级、防时序侧信道攻击、以及 `PinGuard` 防暴力破解线性惩罚退避（5次容错、递增锁定、冷却与完全重置）。
    - **Cron 表达式算法核心 (`CronHelperTests`)**：覆盖 5 段式标准语法校验、非法输入与越界防御、日期与星期区间精准命中（含周日 0/7 双向等价支持）、未来执行时间预测与中文自然语言解释。
    - **模板动态展开引擎 (`TemplateExpanderTests`)**：覆盖内置标准宏（`date`/`time`/`datetime`/`task`）、目录路径宏、自定义日期格式串（`yyyyMMdd` 等）与系统环境变量回退。
    - **微内核生命周期与容错隔离 (`ModuleManagerTests`)**：覆盖模块重复 ID 注册防御、启动序与倒序优雅停机、以及初始化与启动阶段单个模块抛错被隔离标记为 `Faulted` 而宿主与其他模块平稳运行的容错机制。
    - **任务前置条件评估 (`TaskConditionEvaluatorTests`)**：覆盖空条件快速放行、文件存在/不存在探测、环境变量在路径中的自动展开。
  - **测试中发现并修复的两处隐蔽缺陷**：
    - **修复模板引擎将普通单词误当日期格式化的 Bug**：此前 `TemplateExpander` 若遇到含有 `m/d/s/h/y` 等字母的变量（如 `username`、`password` 或普通环境变量）会被底层 `DateTime.ToString()` 强制当成日期格式串并篡改输出；重构为环境变量优先匹配并增加严格的日期占位符词法判定 `IsLikelyDateTimeFormat`，未命中者安全保留原始标记。
    - **修复防爆破锁定输出时延**：`PinGuard.Try` 在第 5 次失败触发锁定瞬间将最新惩罚时长同步输出至 `blockRemaining`，避免调用方首帧拿到零时长。
  - **构建与测试验证**：`dotnet test` 单元测试从 34 项扩充至 62 项，100% 通过（0 失败、0 警告、0 错误）。

- 14:18 进程名称归一化与列表管理统一重构 — `src/Services/ProcessHelper.cs` `src/Services/ProcessExclusionService.cs` `src/Modules/AppAutoMute/AppAutoMuteModule.cs` `src/Modules/Awake/Services/AwakeService.cs` `src/Views/*` `tests/CarroDesk.Tests/*`：
  - **核心重构与格式统一**：抽象公共静态类 `ProcessHelper`，将跨模块的进程名处理收敛为标准统一规则（存储与界面展示统一为带 `.exe` 的小写规范名，如 `chrome.exe`）。
  - **输入容错清洗与批量支持**：无论用户输入纯名字、带 `.exe` 还是完整文件路径，一律经 `Normalize` 自动清洗截取为标准进程名；支持 `,` 与 `;` 批量粘贴并自动不区分大小写去重。
  - **运行时弹性匹配与 Bug 修复**：实现 `IsMatch` 与 `ContainsProcess` 弹性比对（无论历史配置是否包含 `.exe` 均双向命中），彻底修复此前 `AppAutoMuteModule` 若用户输入未加 `.exe` 会导致前台判断完全失效的严重 Bug。
  - **运行中窗口应用发现统一**：提炼 `GetRunningWindowProcesses()`，供配置编辑器、自动静音设置、保持唤醒设置 3 个窗口一键拾取运行中前台应用，自动过滤自身与 Explorer，消除此前 3 处各写一套的重复逻辑。
  - **构建与测试验证**：`dotnet build` 0 警告 0 错误；新增 `ProcessHelperTests` 涵盖归一化、去重、弹性比对与应用枚举等测试，全局 34 项单元测试 100% 通过。

- 14:08 新增保持唤醒 (Awake) 内置模块 — `src/Modules/Awake/*` `src/Views/AwakeSettingsWindow.*` `src/Services/ConfigService.cs` `src/Host/Services/ConfigManager.cs` `src/App.xaml.cs` `src/Assets/Locales/*.json` `tests/CarroDesk.Tests/*`：
  - **核心功能落地**：参考 PowerToys Awake 设计，为 CarroDesk 引入原生轻量级内置模块 `AwakeModule`，阻止系统休眠/睡眠与屏幕熄灭，保障大文件下载、长时间代码编译、视频压制渲染等挂机任务平稳执行。
  - **多工作模式与屏幕分离控制**：
    - 支持 4 种工作模式：关闭（遵循系统默认电源策略）、无限期保持唤醒、定时倒计时保持唤醒（内置 15m/30m/1h/2h/4h/8h 快捷预设）、保持唤醒至指定时刻（如 18:00 下班前）；
    - 屏幕常亮与系统唤醒精细分离：支持勾选“保持屏幕常亮”（系统+屏幕全开，适合看文档与演示）或取消勾选（仅阻止系统休眠，允许显示器按时熄灭省电，对 OLED 友好）。
  - **智能环境感知与电池保护**：
    - **笔记本电池保护**：断开交流电使用电池或电量低于预设阈值（默认 20%）时自动暂停保持唤醒并弹窗通知，防止放包内发热或意外耗尽电量；重接电源自动恢复；
    - **目标进程联动**：支持配置进程名单（如 devenv, blender, ffmpeg 等），检测到运行时自动进入保持唤醒，全部退出后自动恢复关闭。
  - **交互与生态协同**：
    - 遵循 CarroDesk 二级收敛规范，根项动态显示当前状态与剩余时间，同时自动无缝集成于桌面控制面板（FloatingPanel）；
    - 支持全局快捷键（默认 `Win+Shift+W`）一键快速切换，倒计时结束/低电量触发提供桌面气泡通知；
    - 提供专用现代卡片式配置窗口 `AwakeSettingsWindow`，支持运行中进程一键拾取与自定义时间输入。
  - **构建与测试验证**：`dotnet build` 0 警告 0 错误，新增 `AwakeModuleTests` 覆盖契约、菜单结构、模式切换与配置持久化，全局 28 项单元测试 100% 通过。
